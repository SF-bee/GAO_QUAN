#include <bits/stdc++.h>
const int MAX = 1e2;

int n;
int dp[MAX][MAX][MAX];
int a[MAX], b[MAX], c[MAX];

int main(){
    std::ios::sync_with_stdio(0);
    std::cin.tie(0);

    std::cin >> n;
    for(int i = 0; i < n; ++i)
        std::cin >> a[i] >> b[i] >> c[i];
    for(int i = 1;i <= n;i++){
        for(int x = )
    }
    return 0;
}