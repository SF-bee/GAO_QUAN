#include <bits/stdc++.h>

int a, b;
struct frac {
    int son, mum;
    frac operator + (const frac &rhs) const {
        int &son, 
    }
}

int main() {
    std::cin.tie(0) -> sync_with_stdio(0);

    
    return 0;
}